# quantization-fundamentals
A from-scratch, PyTorch-only walkthrough of linear (uniform) quantization — the core idea behind INT8 model compression. No libraries beyond torch; every formula is implemented by hand so you can see exactly what's happening.
